---
phase: 05-verification-housekeeping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/02-indexing-pipeline/02-VERIFICATION.md
  - .planning/phases/03-product/03-VERIFICATION.md
autonomous: true
requirements: [API-01, API-02, API-03, API-04, FE-01, FE-02, FE-03, FE-04]

must_haves:
  truths:
    - "REQUIREMENTS.md checkboxes match the actual completion state of all v1 API and frontend requirements"
    - "A VERIFICATION.md exists for Phase 2 that confirms the indexing pipeline success criteria are satisfied in code"
    - "A VERIFICATION.md exists for Phase 3 that confirms the product success criteria are satisfied in code"
    - "Every Phase 3 requirement (API-01..04, FE-01..04) is traced to at least one verified file or behavior"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Authoritative requirement completion state"
      contains: "[x] **API-01**"
    - path: ".planning/phases/02-indexing-pipeline/02-VERIFICATION.md"
      provides: "Phase 2 static code verification report"
      min_lines: 60
    - path: ".planning/phases/03-product/03-VERIFICATION.md"
      provides: "Phase 3 static code verification report"
      min_lines: 80
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "traceability table"
      via: "requirement ID rows updated to Complete"
      pattern: "API-0[1-4].*Phase [35].*Complete"
    - from: ".planning/phases/03-product/03-VERIFICATION.md"
      to: "api/src/polisi_api/routes/chat.py"
      via: "artifact verification of RAG endpoint"
      pattern: "chat\\.py.*VERIFIED"
---

<objective>
Update REQUIREMENTS.md checkboxes to reflect completed work and write VERIFICATION.md files for Phase 2 and Phase 3 that confirm success criteria are met by inspecting actual code and files.

Purpose: The v1.0 milestone audit found missing VERIFICATION.md files for Phases 2 and 3, and REQUIREMENTS.md still had Phase 3 requirements marked pending even though Phase 3 was fully executed. This plan closes that documentation debt.

Output: Updated REQUIREMENTS.md with all v1 requirements checked; two VERIFICATION.md files (Phase 2 and Phase 3) using the same format as the existing Phase 4 VERIFICATION.md.
</objective>

<execution_context>
@/Users/jasonwee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jasonwee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-fix-source-url-chain/04-VERIFICATION.md
@.planning/phases/02-indexing-pipeline/02-04-SUMMARY.md
@.planning/phases/03-product/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md — tick all completed v1 requirements</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Read the current `.planning/REQUIREMENTS.md`. The following requirements are implemented in code but their checkboxes are still unchecked (`[ ]`). Tick them:

    - **API-01**: `[ ] **API-01**` → `[x] **API-01**`
      Evidence: `api/src/polisi_api/routes/chat.py` POST /api/chat calls `service.handle_chat()` which calls `self._retriever.retrieve()` for vector similarity search and `self._generator.generate()` for Claude answer generation.

    - **API-03**: `[ ] **API-03**` → `[x] **API-03**`
      Evidence: `api/src/polisi_api/chat/detector.py` `detect_language()` is called in `ChatService.handle_chat()` and `generate_reply()`. `build_prompt()` in `prompting.py` receives `language` param. Claude responds in detected language.

    - **API-04**: `[ ] **API-04**` → `[x] **API-04**`
      Evidence: `api/src/polisi_api/chat/repository.py` `ensure_conversation()` and `add_message()` persist to Supabase `conversations` and `messages` tables. `routes/conversations.py` exposes retrieval endpoints.

    - **FE-01**: `[ ] **FE-01**` → `[x] **FE-01**`
      Evidence: `web/app/auth/page.tsx` handles sign-up/sign-in via Supabase Auth. `web/lib/supabase/server.ts` `createServerSupabaseClient()` reads persisted session for server components.

    - **FE-02**: `[ ] **FE-02**` → `[x] **FE-02**`
      Evidence: `web/components/chat/message-composer.tsx` captures user input. `web/components/chat/message-list.tsx` renders messages with inline superscript citation markers.

    - **FE-04**: `[ ] **FE-04**` → `[x] **FE-04**`
      Evidence: `web/components/chat/conversation-sidebar.tsx` renders recent-first history list with click-to-resume. `web/app/(app)/chat/[conversationId]/page.tsx` handles resume routes.

    Also update the **Traceability** table at the bottom of REQUIREMENTS.md — change the `Status` column entries for API-01, API-03, API-04, FE-01, FE-02, FE-04 from `Pending` to `Complete`. The Phase column for all of these should remain `Phase 5 (gap closure)` — that is correct since Phase 5 is formally certifying their completion.

    Update the footer line:
    `*Last updated: 2026-03-01 — Gap closure phases 4-5 added; INDX-03 reset to pending; Phase 3 requirements reassigned to gap closure phases*`
    →
    `*Last updated: 2026-03-01 — Phase 5 housekeeping: all v1 Phase 3 requirements marked Complete*`

    Note: API-02 (`[x]`) and FE-03 (`[x]`) are already checked from Phase 4 — do not modify them.
  </action>
  <verify>
    <automated>grep -c "\[x\]" /Users/jasonwee/VCP/Polisi2/.planning/REQUIREMENTS.md</automated>
    <manual>Confirm output is 18 (12 existing + 6 newly ticked = 18 total checked). Also confirm no `[ ] **API-` or `[ ] **FE-` remain in the file for v1 requirements.</manual>
  </verify>
  <done>REQUIREMENTS.md has all 18 v1 requirements checked. No v1 API or FE requirement remains unchecked. Traceability table shows Complete for all Phase 3 requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Write Phase 2 VERIFICATION.md</name>
  <files>.planning/phases/02-indexing-pipeline/02-VERIFICATION.md</files>
  <action>
    Create `.planning/phases/02-indexing-pipeline/02-VERIFICATION.md` using the same structure as `.planning/phases/04-fix-source-url-chain/04-VERIFICATION.md`.

    Use this frontmatter:
    ```yaml
    ---
    phase: 02-indexing-pipeline
    verified: 2026-03-01T00:00:00Z
    status: passed
    score: 4/4 success criteria verified
    re_verification: false
    gaps: []
    human_verification:
      - test: "Run full indexer against live DO Spaces with credentials in scraper/.env"
        expected: "Supabase documents table populated with embeddings and source metadata for all 4 file types"
        why_human: "Requires live DO Spaces + Supabase + OpenAI credentials not available in this verification environment"
    ---
    ```

    Verify each of the 4 Phase 2 success criteria from ROADMAP.md against actual code:

    **Truth 1:** "Running the indexer against DO Spaces produces populated rows in the Supabase `documents` table with embeddings and source metadata"
    - Verify: Read `scraper/src/polisi_scraper/indexer/pipeline.py` — confirm it reads from Spaces manifest and calls Supabase upsert with embeddings, title, source_url, agency, date.
    - Verify: Read `scraper/src/polisi_scraper/indexer/runner.py` — confirm full/incremental run modes exist.

    **Truth 2:** "Embeddings are generated using OpenAI `text-embedding-3-large` and vector similarity query returns relevant chunks"
    - Verify: Look for `text-embedding-3-large` model string in indexer source files.
    - Verify: Look for `match_documents` or equivalent similarity function call in retrieval code.

    **Truth 3:** "Re-running the indexer on unchanged files skips them; only new or changed files are processed"
    - Verify: Look for `version_token`/`storage_path` uniqueness logic or skip-if-exists guard in pipeline.py or manifest.py.

    **Truth 4:** "All four document types (HTML, PDF, DOCX, XLSX) are parsed without errors"
    - Verify: Check `scraper/src/polisi_scraper/indexer/parsers/` directory — confirm parsers for all 4 types exist.
    - Verify: Run the test suite: `cd /Users/jasonwee/VCP/Polisi2/api && ../.venv313/bin/pytest ../scraper/tests/test_indexer_pipeline.py -v 2>&1 | tail -20` or equivalent.

    Use file reads and grep to gather actual evidence. Report file paths and line numbers like the Phase 4 VERIFICATION.md does. Mark each truth VERIFIED, PARTIAL, or UNCERTAIN. Write the full verification document with sections: Goal Achievement, Required Artifacts, Key Link Verification, Requirements Coverage, Test Suite Results, and Gaps Summary.

    Requirements to trace: INDX-01, INDX-02, INDX-03, INDX-04 — all should be SATISFIED (completed in Phases 2 and 4).
  </action>
  <verify>
    <automated>test -f /Users/jasonwee/VCP/Polisi2/.planning/phases/02-indexing-pipeline/02-VERIFICATION.md && wc -l /Users/jasonwee/VCP/Polisi2/.planning/phases/02-indexing-pipeline/02-VERIFICATION.md</automated>
    <manual>Confirm the file has at least 60 lines and contains "VERIFIED" entries for all 4 success criteria.</manual>
  </verify>
  <done>02-VERIFICATION.md exists with ≥60 lines, covers all 4 Phase 2 success criteria, cites actual file paths and line numbers as evidence, and includes a Requirements Coverage table showing INDX-01..04 all SATISFIED.</done>
</task>

<task type="auto">
  <name>Task 3: Write Phase 3 VERIFICATION.md</name>
  <files>.planning/phases/03-product/03-VERIFICATION.md</files>
  <action>
    Create `.planning/phases/03-product/03-VERIFICATION.md` using the same structure as the Phase 4 VERIFICATION.md.

    Use this frontmatter:
    ```yaml
    ---
    phase: 03-product
    verified: 2026-03-01T00:00:00Z
    status: passed
    score: 5/5 success criteria verified
    re_verification: false
    gaps: []
    human_verification:
      - test: "Full end-to-end browser test: sign up, ask a question in BM, receive cited answer, click citation, view history"
        expected: "All 5 success criteria satisfied in live browser session"
        why_human: "Requires live Supabase Auth + API credentials and a browser session; cannot verify UI interaction programmatically"
    ---
    ```

    Verify each of the 5 Phase 3 success criteria from ROADMAP.md against actual code:

    **Truth 1:** "User can sign up with email and password, log in, and remain logged in across browser refresh"
    - Verify: `web/app/auth/page.tsx` — Supabase Auth sign-up/sign-in present.
    - Verify: `web/lib/supabase/server.ts` — `createServerSupabaseClient` persists session via cookies.
    - Verify: `web/middleware.ts` or layout — auth-first routing enforced.

    **Truth 2:** "User can type a question in Bahasa Malaysia and receive an answer in Bahasa Malaysia"
    - Verify: `api/src/polisi_api/chat/detector.py` — `detect_language()` present.
    - Verify: `api/src/polisi_api/chat/prompting.py` — `build_prompt()` takes `language` param and injects BM/EN instructions.
    - Verify: `api/src/polisi_api/chat/service.py` — language is detected and passed through.

    **Truth 3:** "Every answer contains inline superscript citation numbers and clicking opens the original source document URL"
    - Verify: `web/components/chat/message-list.tsx` — inline `[N]` markers rendered.
    - Verify: `web/components/chat/citation-panel.tsx` — `<a href={citation.source_url}>` present (source_url nullable per Phase 4 fix).
    - Verify: `api/src/polisi_api/models.py` — `AssistantResponse.citations: list[CitationRecord]` present.

    **Truth 4:** "User can view a sidebar list of past conversation sessions and click any to resume it with full message history"
    - Verify: `web/components/chat/conversation-sidebar.tsx` — conversation list rendered with click-to-navigate.
    - Verify: `web/app/(app)/chat/[conversationId]/page.tsx` — resume route exists.
    - Verify: `api/src/polisi_api/routes/conversations.py` — GET conversation/messages endpoints present.

    **Truth 5:** "A question with no matching documents returns a graceful 'no information found' response rather than a hallucinated answer"
    - Verify: `api/src/polisi_api/chat/service.py` `generate_reply()` — `build_no_information_text()` called when retrieved is empty or similarity < threshold.
    - Verify: `api/src/polisi_api/chat/prompting.py` — `build_no_information_text()` function exists.

    Use actual file reads and grep for evidence. Report file paths, line numbers, and code patterns as evidence (same style as Phase 4 VERIFICATION.md). Write full verification document with sections: Goal Achievement, Required Artifacts, Key Link Verification, Requirements Coverage, Test Suite Results, Gaps Summary.

    Requirements to trace: API-01, API-02, API-03, API-04, FE-01, FE-02, FE-03, FE-04.
    - API-02 and FE-03 were formally verified in Phase 4 — note this in the report and mark SATISFIED.
    - All others should be verified by reading the Phase 3 code artifacts listed above.
  </action>
  <verify>
    <automated>test -f /Users/jasonwee/VCP/Polisi2/.planning/phases/03-product/03-VERIFICATION.md && wc -l /Users/jasonwee/VCP/Polisi2/.planning/phases/03-product/03-VERIFICATION.md</automated>
    <manual>Confirm the file has at least 80 lines and contains "VERIFIED" entries for all 5 success criteria and all 8 requirement IDs.</manual>
  </verify>
  <done>03-VERIFICATION.md exists with ≥80 lines, covers all 5 Phase 3 success criteria with code evidence, and includes a Requirements Coverage table showing API-01..04 and FE-01..04 all SATISFIED.</done>
</task>

</tasks>

<verification>
1. `grep -c "\[x\]" /Users/jasonwee/VCP/Polisi2/.planning/REQUIREMENTS.md` returns 18
2. `test -f /Users/jasonwee/VCP/Polisi2/.planning/phases/02-indexing-pipeline/02-VERIFICATION.md` exits 0
3. `test -f /Users/jasonwee/VCP/Polisi2/.planning/phases/03-product/03-VERIFICATION.md` exits 0
4. `grep "VERIFIED\|SATISFIED" /Users/jasonwee/VCP/Polisi2/.planning/phases/02-indexing-pipeline/02-VERIFICATION.md | wc -l` returns ≥4
5. `grep "VERIFIED\|SATISFIED" /Users/jasonwee/VCP/Polisi2/.planning/phases/03-product/03-VERIFICATION.md | wc -l` returns ≥8
</verification>

<success_criteria>
- REQUIREMENTS.md: 18 checkboxes ticked, no v1 requirement unchecked, traceability table updated
- 02-VERIFICATION.md: Exists with ≥60 lines, 4 truths verified, INDX-01..04 traced
- 03-VERIFICATION.md: Exists with ≥80 lines, 5 truths verified, API-01..04 + FE-01..04 traced
</success_criteria>

<output>
After completion, create `.planning/phases/05-verification-housekeeping/05-01-SUMMARY.md`
</output>
