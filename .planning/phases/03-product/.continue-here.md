---
phase: 03-product
task: 2
total_tasks: 3
status: in_progress
last_updated: 2026-02-28T15:42:58.150Z
---

<current_state>
Phase 3 execution is complete (all 4 summaries committed), and work is now in post-phase verification hardening.

Current verification state:
- API starts successfully on `127.0.0.1:8000` and `/healthz` returns 200.
- Web starts successfully on `127.0.0.1:3001` (port 3000 is occupied by an unrelated local Ruby process).
- Web routing check passes (`/` redirects to `/auth`, `/auth` returns 200).
- Authenticated `/api/conversations` call passes against Supabase after migrations were applied.
- Authenticated `/api/chat` currently fails with OpenAI embeddings `429 Too Many Requests` (runtime/provider quota issue, not schema/auth wiring).
</current_state>

<completed_work>

- Task 1: Validate runtime environment and database state - Done
  - Confirmed Supabase tables now exist: `documents`, `conversations`, `messages`, `citations`.
  - Confirmed API/web env files are populated.
- Task 2: Fix local runtime blockers found during smoke checks - In progress
  - Fixed API env parsing for `API_ALLOWED_ORIGINS` by adding `NoDecode` in `api/src/polisi_api/config.py`.
  - Fixed Next.js 15 async cookies usage by making `createServerSupabaseClient()` async and updating all app call sites.
</completed_work>

<remaining_work>

- Task 2: Commit the smoke-fix code changes
  - `api/src/polisi_api/config.py`
  - `web/lib/supabase/server.ts`
  - `web/app/page.tsx`
  - `web/app/auth/page.tsx`
  - `web/app/(app)/layout.tsx`
  - `web/app/(app)/chat/page.tsx`
  - `web/app/(app)/chat/[conversationId]/page.tsx`
- Task 3: Handle or document `/api/chat` OpenAI 429 behavior
  - Preferred: add graceful fallback/response mapping for provider 429 in chat path.
  - Then rerun authenticated `/api/chat` smoke test.
</remaining_work>

<decisions_made>

- Keep Phase 3 completion commits intact; post-phase smoke fixes are handled as separate follow-up changes.
- Treat `429` from embeddings provider as an external runtime constraint, not a schema/auth defect.
- Use port `3001` for local web verification when `3000` is already in use by unrelated processes.
</decisions_made>

<blockers>
- OpenAI embeddings request currently returns HTTP 429 during live `/api/chat` smoke test.
- `api/.env.example` and `web/.env.example` currently show as deleted in working tree; confirm whether deletion was intentional rename behavior before next commit.
</blockers>

<context>
Work rhythm is good: core product is shipped and validated on auth/routing/history paths. Remaining effort is narrow and operational:
1) checkpoint commit for smoke-fix code,
2) handle provider-rate-limit path in chat runtime (or clearly document as expected external failure in smoke), and
3) rerun the same probes to close verification cleanly.

Avoid broad refactors; keep final changes minimal and directly tied to verification robustness.
</context>

<files_modified>
- `api/.env.example` (deleted, uncommitted)
- `api/src/polisi_api/config.py` (modified, uncommitted)
- `web/.env.example` (deleted, uncommitted)
- `web/lib/supabase/server.ts` (modified, uncommitted)
- `web/app/page.tsx` (modified, uncommitted)
- `web/app/auth/page.tsx` (modified, uncommitted)
- `web/app/(app)/layout.tsx` (modified, uncommitted)
- `web/app/(app)/chat/page.tsx` (modified, uncommitted)
- `web/app/(app)/chat/[conversationId]/page.tsx` (modified, uncommitted)
</files_modified>

<next_action>
Start with: commit the current smoke-fix code changes (API NoDecode + async cookies call sites), then patch `/api/chat` to handle OpenAI 429 gracefully and rerun the authenticated chat smoke probe.
</next_action>
