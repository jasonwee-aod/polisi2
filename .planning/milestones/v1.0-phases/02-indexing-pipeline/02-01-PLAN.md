---
phase: 02-indexing-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scraper/pyproject.toml
  - scraper/.env.example
  - scraper/README.md
  - scraper/src/polisi_scraper/config.py
  - scraper/src/polisi_scraper/indexer/__init__.py
  - scraper/src/polisi_scraper/indexer/manifest.py
  - scraper/src/polisi_scraper/indexer/state.py
  - scraper/tests/test_indexer_manifest.py
autonomous: true
requirements:
  - INDX-01
  - INDX-04
must_haves:
    truths:
      - "Indexer runtime can enumerate raw DO Spaces objects and normalize them into parse-ready work items with source metadata."
      - "Unchanged files are rejected before parse/embed work by comparing storage identity and SHA/version fingerprints."
      - "Indexer configuration covers Spaces, Supabase, and OpenAI access without hardcoded secrets."
    artifacts:
      - path: "scraper/src/polisi_scraper/config.py"
        provides: "Unified runtime settings for scraper + indexer execution"
        contains: "class ScraperSettings"
      - path: "scraper/src/polisi_scraper/indexer/manifest.py"
        provides: "Corpus manifest loader that lists raw files from DO Spaces"
        contains: "class SpacesCorpusManifest"
      - path: "scraper/src/polisi_scraper/indexer/state.py"
        provides: "Fingerprint and pending-work state contracts for incremental indexing"
        contains: "class IndexedFingerprintStore"
      - path: "scraper/tests/test_indexer_manifest.py"
        provides: "Regression coverage for manifest normalization and unchanged-file detection"
        contains: "def test_pending_items_skip_existing_sha"
    key_links:
      - from: "scraper/src/polisi_scraper/config.py"
        to: "scraper/src/polisi_scraper/indexer/manifest.py"
        via: "DO Spaces and Supabase/OpenAI settings injected into manifest loader"
      - from: "scraper/src/polisi_scraper/indexer/manifest.py"
        to: "scraper/src/polisi_scraper/indexer/state.py"
        via: "manifest entries are filtered into pending vs unchanged work items"
      - from: "scraper/src/polisi_scraper/indexer/state.py"
        to: "scraper/tests/test_indexer_manifest.py"
        via: "state and fingerprint edge cases are locked by deterministic tests"
---

<objective>
Create the Phase 2 foundation so indexing inputs, secrets, and incremental-work contracts are fixed before parser and embedding implementation.

Purpose: Prevent downstream rework by defining one consistent source inventory and skip strategy for all later indexing code.
Output: Indexer dependencies/config, Spaces manifest loader, and pending-work state abstractions with tests.
</objective>

<execution_context>
@/Users/jasonwee/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jasonwee/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-indexing-pipeline/02-CONTEXT.md
@infrastructure.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend package/runtime settings for indexing work</name>
  <files>scraper/pyproject.toml, scraper/.env.example, scraper/README.md, scraper/src/polisi_scraper/config.py</files>
  <action>Add the Phase 2 dependencies and environment contract needed for indexing: LlamaIndex core/OpenAI integration, file-type parser libraries (`pypdf`, `python-docx`, `openpyxl`), and whichever Postgres/Supabase client the indexer will use later. Expand the existing settings loader to cover `OPENAI_API_KEY`, indexer batching limits, and a direct Supabase/Postgres connection string if the persistence layer needs one. Keep the configuration centralized in `config.py` and document every new variable in `.env.example` and `README.md`.</action>
  <verify><automated>python -m pytest scraper/tests/test_indexer_manifest.py::test_indexer_settings_validation -q</automated></verify>
  <done>Indexer dependencies are declared, new environment variables are documented, and missing indexer secrets fail fast at startup instead of surfacing later in the pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: Build Spaces corpus manifest and fingerprint interfaces</name>
  <files>scraper/src/polisi_scraper/indexer/__init__.py, scraper/src/polisi_scraper/indexer/manifest.py, scraper/src/polisi_scraper/indexer/state.py</files>
  <action>Create `SpacesCorpusManifest` that lists raw objects under the agreed `gov-my/{agency}/{year-month}/filename.ext` prefix and normalizes each object into a source record with file type, storage path, agency, version identity, and any recoverable metadata. Define an `IndexedFingerprintStore` abstraction that can answer whether a given storage path + SHA/version has already been indexed, but do not wire it to the final database implementation yet. The result of this task should be a `PendingIndexItem`-style contract that later parser and persistence steps can consume directly.</action>
  <verify><automated>python -m pytest scraper/tests/test_indexer_manifest.py::test_spaces_manifest_normalizes_storage_objects -q</automated></verify>
  <done>DO Spaces objects can be enumerated and transformed into deterministic pending-index work items with a clear hook for unchanged-file skipping.</done>
</task>

<task type="auto">
  <name>Task 3: Lock manifest and incremental-skip behavior with tests</name>
  <files>scraper/tests/test_indexer_manifest.py</files>
  <action>Add focused tests for settings validation, file-type inference from object keys, manifest ordering, and unchanged-file skip decisions when a storage path and SHA/version already exist in the fingerprint store. Keep tests deterministic with fake Spaces listings and in-memory state doubles so the executor can validate logic without live cloud calls.</action>
  <verify><automated>python -m pytest scraper/tests/test_indexer_manifest.py -q</automated></verify>
  <done>The Phase 2 foundation has a fast regression suite proving the indexer can discover corpus inputs and avoid reprocessing unchanged files.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] python -m pytest scraper/tests/test_indexer_manifest.py -q
- [ ] rg "OPENAI_API_KEY|SUPABASE_DB_URL|INDEXER_" scraper/.env.example scraper/src/polisi_scraper/config.py
- [ ] rg "class SpacesCorpusManifest|class IndexedFingerprintStore" scraper/src/polisi_scraper/indexer/manifest.py scraper/src/polisi_scraper/indexer/state.py
</verification>

<success_criteria>

- All tasks completed
- Indexer dependencies and runtime settings are documented and validated
- DO Spaces objects can be normalized into pending work items
- Incremental skip logic has deterministic regression coverage before parser work starts
  </success_criteria>

<output>
After completion, create `.planning/phases/02-indexing-pipeline/02-01-SUMMARY.md`
</output>
