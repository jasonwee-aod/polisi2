---
phase: 02-indexing-pipeline
plan: 04
type: execute
wave: 4
depends_on:
  - "02-03"
files_modified:
  - infra/droplet/setup_runtime.sh
  - infra/droplet/systemd/polisi-scraper.service
  - infra/droplet/systemd/polisi-indexer-placeholder.service
  - infra/droplet/RUNBOOK.md
  - scraper/scripts/preflight_check.py
  - scraper/src/polisi_scraper/indexer/runner.py
  - scraper/README.md
autonomous: true
requirements:
  - INDX-04
must_haves:
    truths:
      - "A droplet operator can run the indexer manually after a scrape and prove only new/changed files are processed on rerun."
      - "The existing scraper runtime is extended to include parser/embedding dependencies and indexer preflight checks."
      - "Operational docs capture the exact commands to run the first full index, validate BM/EN retrieval, and verify incremental behavior."
    artifacts:
      - path: "infra/droplet/systemd/polisi-indexer-placeholder.service"
        provides: "Real indexer service definition replacing the Phase 1 placeholder behavior"
        contains: "polisi_scraper.indexer.runner"
      - path: "scraper/scripts/preflight_check.py"
        provides: "Runtime validator covering indexer secrets and dependencies before execution"
        contains: "def run_preflight"
      - path: "infra/droplet/RUNBOOK.md"
        provides: "Operational runbook for first full index, reruns, and retrieval smoke checks"
        contains: "incremental rerun"
      - path: "infra/droplet/systemd/polisi-scraper.service"
        provides: "Scraper/indexer handoff path on the droplet"
        contains: "ExecStartPost"
    key_links:
      - from: "infra/droplet/setup_runtime.sh"
        to: "scraper/scripts/preflight_check.py"
        via: "runtime packages are installed before the preflight validator runs"
      - from: "infra/droplet/systemd/polisi-scraper.service"
        to: "infra/droplet/systemd/polisi-indexer-placeholder.service"
        via: "scraper completion triggers the real indexer handoff on the droplet"
      - from: "infra/droplet/RUNBOOK.md"
        to: "scraper/src/polisi_scraper/indexer/runner.py"
        via: "operator instructions use the shipped CLI for full and incremental runs"
---

<objective>
Operationalize the Phase 2 indexer on the droplet and document how to validate incremental behavior end-to-end.

Purpose: The pipeline only creates product value once it can run repeatably in the target environment and prove unchanged files are skipped on reruns.
Output: Runtime updates, real indexer service wiring, preflight validation, and an operator runbook for full and incremental indexing.
</objective>

<execution_context>
@/Users/jasonwee/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jasonwee/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-indexing-pipeline/02-CONTEXT.md
@.planning/phases/02-indexing-pipeline/02-03-PLAN.md
@infrastructure.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend droplet runtime and preflight checks for indexing</name>
  <files>infra/droplet/setup_runtime.sh, scraper/scripts/preflight_check.py, scraper/README.md</files>
  <action>Update the droplet bootstrap path so all parser and embedding dependencies required by Phase 2 are installed reproducibly. Extend `preflight_check.py` to validate the new indexer env vars, database connectivity path, and importability of the parser/embedding stack before a run starts. Keep the README aligned with the same runtime expectations so local and droplet execution follow one documented path.</action>
  <verify><automated>python scraper/scripts/preflight_check.py --components indexer --dry-run</automated></verify>
  <done>The runtime setup and preflight path can catch missing indexer dependencies or secrets before the operator starts a long indexing job.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the placeholder indexer service and wire scraper handoff</name>
  <files>infra/droplet/systemd/polisi-scraper.service, infra/droplet/systemd/polisi-indexer-placeholder.service, scraper/src/polisi_scraper/indexer/runner.py</files>
  <action>Turn the Phase 1 placeholder service into a real one-shot indexer execution path that invokes the shipped Phase 2 CLI. Update the scraper service handoff so a successful scrape can trigger indexing on the same droplet without inventing a second manual orchestration path. Ensure the runner supports bounded/manual options for debugging and full-run mode for production handoff.</action>
  <verify><automated>python -m pytest scraper/tests/test_indexer_pipeline.py::test_runner_incremental_mode_flags -q</automated></verify>
  <done>The droplet has a real indexer service path and the runner exposes the switches needed for both manual debugging and production reruns.</done>
</task>

<task type="auto">
  <name>Task 3: Write the Phase 2 operational runbook and acceptance checklist</name>
  <files>infra/droplet/RUNBOOK.md</files>
  <action>Document the full operational flow: first full index, BM query smoke, English query smoke, and a second run proving unchanged files are skipped while new/changed files are processed. Include exact commands, expected log locations, and an acceptance checklist tied directly to the four Phase 2 success criteria. Keep the runbook focused on the target droplet path rather than generic cloud alternatives.</action>
  <verify><automated>rg "text-embedding-3-large|incremental rerun|Bahasa Malaysia|English|Phase 2" infra/droplet/RUNBOOK.md</automated></verify>
  <done>An operator can provision, run, validate, and rerun the indexer on the droplet without guessing the commands or acceptance checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] python scraper/scripts/preflight_check.py --components indexer --dry-run
- [ ] python -m pytest scraper/tests/test_indexer_pipeline.py::test_runner_incremental_mode_flags -q
- [ ] rg "ExecStartPost|polisi_scraper.indexer.runner" infra/droplet/systemd/polisi-scraper.service infra/droplet/systemd/polisi-indexer-placeholder.service
- [ ] rg "incremental rerun|Bahasa Malaysia|English" infra/droplet/RUNBOOK.md
</verification>

<success_criteria>

- All tasks completed
- Droplet runtime and preflight cover the full Phase 2 dependency set
- Scraper-to-indexer handoff exists as a real service path
- Runbook proves full indexing, BM/EN retrieval smoke, and incremental rerun validation
  </success_criteria>

<output>
After completion, create `.planning/phases/02-indexing-pipeline/02-04-SUMMARY.md`
</output>
