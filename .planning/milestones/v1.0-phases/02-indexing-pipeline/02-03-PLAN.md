---
phase: 02-indexing-pipeline
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - supabase/migrations/20260228_02_phase2_documents_chunks.sql
  - scraper/src/polisi_scraper/indexer/embeddings.py
  - scraper/src/polisi_scraper/indexer/store.py
  - scraper/src/polisi_scraper/indexer/pipeline.py
  - scraper/src/polisi_scraper/indexer/runner.py
  - scraper/scripts/query_smoke.py
  - scraper/tests/test_indexer_pipeline.py
  - scraper/README.md
autonomous: true
requirements:
  - INDX-01
  - INDX-02
  - INDX-03
  - INDX-04
user_setup:
  - service: openai
    why: "The embedding client requires a project API key that Claude cannot create"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI dashboard -> API keys"
  - service: supabase
    why: "The indexer may need a direct Postgres connection string for pgvector writes and similarity smoke queries"
    env_vars:
      - name: SUPABASE_DB_URL
        source: "Supabase dashboard -> Project Settings -> Database connection string"
must_haves:
    truths:
      - "The LlamaIndex-based pipeline reads pending raw files from DO Spaces, parses them, chunks them, embeds them, and persists the results in one runnable flow."
      - "Chunks and embeddings are written into Supabase `documents` rows together with core source metadata and version identity."
      - "Embeddings use OpenAI `text-embedding-3-large`, matching the multilingual BM/EN retrieval decision."
      - "A BM or English vector similarity smoke query can return relevant stored chunks after indexing."
      - "Incremental completion state is only advanced after successful persistence so reruns skip unchanged files without losing failed work."
    artifacts:
      - path: "supabase/migrations/20260228_02_phase2_documents_chunks.sql"
        provides: "Schema updates that allow multiple chunk rows per document version and a reusable similarity query"
        contains: "create or replace function public.match_documents"
      - path: "scraper/src/polisi_scraper/indexer/embeddings.py"
        provides: "Embedding client fixed to the required OpenAI model"
        contains: "text-embedding-3-large"
      - path: "scraper/src/polisi_scraper/indexer/store.py"
        provides: "Supabase/Postgres persistence layer for chunk rows and fingerprints"
        contains: "class DocumentsStore"
      - path: "scraper/src/polisi_scraper/indexer/pipeline.py"
        provides: "End-to-end parse -> embed -> persist orchestration"
        contains: "class IndexingPipeline"
      - path: "scraper/scripts/query_smoke.py"
        provides: "BM/EN retrieval smoke command against stored vectors"
        contains: "def main"
    key_links:
      - from: "supabase/migrations/20260228_02_phase2_documents_chunks.sql"
        to: "scraper/src/polisi_scraper/indexer/store.py"
        via: "store writes and similarity queries target the migrated chunk-row schema"
      - from: "scraper/src/polisi_scraper/indexer/embeddings.py"
        to: "scraper/src/polisi_scraper/indexer/pipeline.py"
        via: "pipeline batches parsed chunks through the OpenAI embedding client"
      - from: "scraper/src/polisi_scraper/indexer/pipeline.py"
        to: "scraper/scripts/query_smoke.py"
        via: "query smoke validates that indexed chunks are retrievable after a pipeline run"
---

<objective>
Wire the actual indexing pipeline so parsed chunks become searchable vectors in Supabase.

Purpose: Phase 2 does not count as done until chunk rows, embeddings, and retrieval proof exist end-to-end instead of isolated parser code.
Output: Supabase chunk-schema migration, embedding/persistence pipeline, indexer runner, and BM/EN retrieval smoke tooling.
</objective>

<execution_context>
@/Users/jasonwee/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jasonwee/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-indexing-pipeline/02-CONTEXT.md
@.planning/phases/02-indexing-pipeline/02-01-PLAN.md
@.planning/phases/02-indexing-pipeline/02-02-PLAN.md
@infrastructure.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the documents schema for chunk-row storage and retrieval</name>
  <files>supabase/migrations/20260228_02_phase2_documents_chunks.sql</files>
  <action>Add a Phase 2 migration that converts `public.documents` from the Phase 1 one-row-per-document assumption into a chunk-row model that can store many chunks for the same source version. Drop or replace the current uniqueness rules on `(source_url, sha256)` and `storage_path` so multiple chunk rows per version are valid, then create the right uniqueness/index strategy such as `(storage_path, chunk_index)` or equivalent. Include a reusable `match_documents` similarity function or SQL helper the smoke query can call later, and make sure old indexed versions remain queryable instead of being overwritten.</action>
  <verify><automated>python -m pytest scraper/tests/test_indexer_pipeline.py::test_documents_schema_supports_multiple_chunks_per_version -q</automated></verify>
  <done>The database schema can persist multiple chunk rows per source version and exposes a predictable vector similarity query path for later smoke tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement embedding, persistence, and indexer runner wiring</name>
  <files>scraper/src/polisi_scraper/indexer/embeddings.py, scraper/src/polisi_scraper/indexer/store.py, scraper/src/polisi_scraper/indexer/pipeline.py, scraper/src/polisi_scraper/indexer/runner.py, scraper/README.md</files>
  <action>Implement the end-to-end indexing pipeline that consumes pending manifest items, parses them with the Phase 2 parser layer, chunks them, embeds each chunk using OpenAI `text-embedding-3-large`, and writes chunk rows plus source metadata into Supabase. Preserve version identity (storage path + SHA/version) and update the fingerprint store only after persistence succeeds. Add a runnable CLI/entrypoint in `runner.py` that supports bounded indexing runs for test execution as well as full corpus runs on the droplet.</action>
  <verify><automated>python -m pytest scraper/tests/test_indexer_pipeline.py::test_indexing_pipeline_persists_chunks_and_fingerprints -q</automated></verify>
  <done>The indexer can run from a CLI, generate embeddings with the correct model, persist searchable chunks to Supabase, and record completion state for incremental runs.</done>
</task>

<task type="auto">
  <name>Task 3: Add BM/EN retrieval smoke tooling and pipeline regression tests</name>
  <files>scraper/scripts/query_smoke.py, scraper/tests/test_indexer_pipeline.py</files>
  <action>Create a query smoke command that runs a vector similarity lookup against stored embeddings for either a BM or English query and prints chunk title/agency/source metadata from the top results. Add tests covering the required model name, schema assumptions, chunk persistence, and retrieval smoke behavior with deterministic fixtures or stubs so the executor can validate the pipeline without depending on live production data during every run.</action>
  <verify><automated>python -m pytest scraper/tests/test_indexer_pipeline.py -q</automated></verify>
  <done>Phase 2 now has automated proof that the embedding pipeline stores retrievable chunks and that BM/EN smoke queries return expected metadata-bearing results.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] python -m pytest scraper/tests/test_indexer_pipeline.py -q
- [ ] rg "text-embedding-3-large" scraper/src/polisi_scraper/indexer/embeddings.py
- [ ] rg "match_documents|vector_cosine_ops" supabase/migrations/20260228_02_phase2_documents_chunks.sql
</verification>

<success_criteria>

- All tasks completed
- Supabase schema supports chunk rows for the same source version
- Indexer runner writes embeddings and metadata into `public.documents`
- BM and English retrieval smoke coverage exists for stored vectors
  </success_criteria>

<output>
After completion, create `.planning/phases/02-indexing-pipeline/02-03-SUMMARY.md`
</output>
