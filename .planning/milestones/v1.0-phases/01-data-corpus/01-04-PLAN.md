---
phase: 01-data-corpus
plan: 04
type: execute
wave: 4
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
files_modified:
  - infra/droplet/setup_runtime.sh
  - infra/droplet/install_playwright.sh
  - infra/droplet/cron/scraper_every_3_days.cron
  - infra/droplet/systemd/polisi-scraper.service
  - infra/droplet/systemd/polisi-indexer-placeholder.service
  - scraper/scripts/preflight_check.py
  - infra/droplet/RUNBOOK.md
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
user_setup:
  - service: digitalocean
    why: "Droplet/Spaces credentials must be created in DigitalOcean dashboard"
    env_vars:
      - name: DO_SPACES_KEY
        source: "DigitalOcean Dashboard -> Spaces -> API Keys"
      - name: DO_SPACES_SECRET
        source: "DigitalOcean Dashboard -> Spaces -> API Keys"
      - name: DO_SPACES_BUCKET
        source: "Existing Space name in DigitalOcean account"
      - name: DO_SPACES_REGION
        source: "Region of the created Space (e.g., sgp1)"
    dashboard_config:
      - task: "Create/choose a Droplet and Spaces bucket"
        location: "DigitalOcean Dashboard"
        details: "Ubuntu droplet with Python 3.11+, outbound internet, and cron enabled"
  - service: supabase
    why: "Database credentials are required for metadata persistence and schema migration"
    env_vars:
      - name: SUPABASE_URL
        source: "Supabase Project Settings -> API URL"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Project Settings -> API keys -> service_role"
must_haves:
    truths:
      - "Scraper runtime can be provisioned on a DigitalOcean Droplet using one documented setup path."
      - "A manual scraper run command succeeds on droplet after setup and preflight checks."
      - "Cron is configured to trigger scraper at 9:00 AM MYT every 3 days without manual intervention."
    artifacts:
      - path: "infra/droplet/setup_runtime.sh"
        provides: "Automated runtime provisioning for droplet"
        contains: "python3"
      - path: "scraper/scripts/preflight_check.py"
        provides: "Environment/dependency validation before run"
        contains: "def run_preflight"
      - path: "infra/droplet/cron/scraper_every_3_days.cron"
        provides: "Cron schedule for recurring scraper execution"
        contains: "0 1 */3 * *"
      - path: "infra/droplet/RUNBOOK.md"
        provides: "Operator runbook for deploy, manual run, and troubleshooting"
        contains: "9:00 AM MYT"
    key_links:
      - from: "infra/droplet/setup_runtime.sh"
        to: "scraper/scripts/preflight_check.py"
        via: "runtime dependencies installed before validation"
      - from: "infra/droplet/cron/scraper_every_3_days.cron"
        to: "scraper/src/polisi_scraper/runner.py"
        via: "scheduled command executes scraper entrypoint"
      - from: "infra/droplet/RUNBOOK.md"
        to: "infra/droplet/systemd/polisi-scraper.service"
        via: "operational instructions for service control and logs"
---

<objective>
Operationalize Phase 1 on DigitalOcean so scraper execution is repeatable and scheduled.

Purpose: Ensure the data corpus pipeline can run reliably in production-like infrastructure before Phase 2 indexing work.
Output: Provisioning scripts, cron/service configuration, preflight checks, and operator runbook.
</objective>

<execution_context>
@/Users/jasonwee/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jasonwee/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-data-corpus/01-CONTEXT.md
@.planning/phases/01-data-corpus/01-03-PLAN.md
@infrastructure.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create droplet provisioning and service assets</name>
  <files>infra/droplet/setup_runtime.sh, infra/droplet/install_playwright.sh, infra/droplet/systemd/polisi-scraper.service, infra/droplet/systemd/polisi-indexer-placeholder.service</files>
  <action>Write scripts to provision Python runtime, OS packages, Playwright dependencies, and service files for scraper execution. Include an indexer placeholder service definition to reserve the runtime path for Phase 2 without implementing indexer logic yet.</action>
  <verify><automated>bash -n infra/droplet/setup_runtime.sh && bash -n infra/droplet/install_playwright.sh</automated></verify>
  <done>Provisioning and service files are syntactically valid and document the intended runtime structure for scraper + upcoming indexer.</done>
</task>

<task type="auto">
  <name>Task 2: Add preflight validator and cron schedule</name>
  <files>scraper/scripts/preflight_check.py, infra/droplet/cron/scraper_every_3_days.cron</files>
  <action>Implement `preflight_check.py` to validate required env vars, connectivity targets, and import/runtime dependencies before a run starts. Add cron file that runs scraper every 3 days at 01:00 UTC (equivalent to 9:00 AM MYT UTC+8), with logs redirected for troubleshooting.</action>
  <verify><automated>python scraper/scripts/preflight_check.py --dry-run && rg "0 1 \\*/3 \\* \\*" infra/droplet/cron/scraper_every_3_days.cron</automated></verify>
  <done>Preflight script can run in dry mode and cron expression is fixed to the required cadence/timezone conversion.</done>
</task>

<task type="auto">
  <name>Task 3: Write deployment/runbook for manual and scheduled operations</name>
  <files>infra/droplet/RUNBOOK.md</files>
  <action>Document step-by-step droplet bootstrap, secret injection, migration command, manual `python scraper.py`-equivalent run invocation, cron install, and rollback/troubleshooting commands. Include acceptance checklist for INFRA-01 and INFRA-02 and explicit timezone note (UTC vs MYT).</action>
  <verify><automated>rg "INFRA-01|INFRA-02|9:00 AM MYT|manual" infra/droplet/RUNBOOK.md</automated></verify>
  <done>Runbook enables a first-time operator to provision, validate, and schedule the scraper without guessing missing steps.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] bash -n infra/droplet/setup_runtime.sh
- [ ] python scraper/scripts/preflight_check.py --dry-run
- [ ] rg "0 1 \\*/3 \\* \\*" infra/droplet/cron/scraper_every_3_days.cron
- [ ] rg "9:00 AM MYT" infra/droplet/RUNBOOK.md
</verification>

<success_criteria>

- All tasks completed
- Droplet setup scripts and service units are ready for execution
- Preflight and cron assets enforce required runtime and schedule
- Runbook captures manual run and scheduled operation acceptance checks
  </success_criteria>

<output>
After completion, create `.planning/phases/01-data-corpus/01-04-SUMMARY.md`
</output>
