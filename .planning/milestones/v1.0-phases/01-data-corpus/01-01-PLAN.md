---
phase: 01-data-corpus
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scraper/pyproject.toml
  - scraper/.env.example
  - scraper/README.md
  - scraper/src/polisi_scraper/config.py
  - scraper/src/polisi_scraper/models.py
  - supabase/migrations/20260228_01_phase1_schema.sql
  - scraper/tests/test_config_and_models.py
autonomous: true
requirements:
  - DB-01
must_haves:
    truths:
      - "Supabase schema for documents, conversations, messages, and citations can be applied without manual edits."
      - "Scraper configuration loads from environment variables and fails fast on missing required credentials."
      - "Document metadata model supports DO Spaces hierarchy `gov-my/{agency}/{year-month}/filename.ext`."
    artifacts:
      - path: "supabase/migrations/20260228_01_phase1_schema.sql"
        provides: "Phase 1 relational schema and vector-ready documents table"
        contains: "create table if not exists public.documents"
      - path: "scraper/src/polisi_scraper/config.py"
        provides: "Centralized settings loader for scraper runtime"
        contains: "class ScraperSettings"
      - path: "scraper/src/polisi_scraper/models.py"
        provides: "Typed metadata contracts used by adapters and uploader"
        contains: "class DocumentRecord"
    key_links:
      - from: "scraper/src/polisi_scraper/config.py"
        to: "scraper/.env.example"
        via: "environment variable names and defaults"
      - from: "scraper/src/polisi_scraper/models.py"
        to: "supabase/migrations/20260228_01_phase1_schema.sql"
        via: "field mapping for persisted metadata"
---

<objective>
Create the Phase 1 foundation so schema, config, and metadata contracts are fixed before scraper implementation.

Purpose: Prevent downstream rework by locking data contracts and operational configuration first.
Output: Supabase migration, scraper project scaffold, validated settings, and metadata models.
</objective>

<execution_context>
@/Users/jasonwee/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jasonwee/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-data-corpus/01-CONTEXT.md
@infrastructure.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold scraper package and environment contract</name>
  <files>scraper/pyproject.toml, scraper/.env.example, scraper/README.md, scraper/src/polisi_scraper/config.py</files>
  <action>Create a Python package scaffold for the scraper with pinned dependencies (`requests`, `beautifulsoup4`, `playwright`, `boto3`, `pydantic`, `pytest`) and a settings loader (`ScraperSettings`) that validates required keys for DO Spaces, Supabase, and crawler runtime. Keep configuration centralized in `config.py` and avoid hardcoded credentials anywhere else.</action>
  <verify><automated>python -m pytest scraper/tests/test_config_and_models.py::test_settings_validation -q</automated></verify>
  <done>Scraper package installs locally, `.env.example` documents all required variables, and missing required env vars produce deterministic startup errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create Supabase migration for v1 core tables</name>
  <files>supabase/migrations/20260228_01_phase1_schema.sql</files>
  <action>Write a migration for `documents`, `conversations`, `messages`, and `citations` with primary/foreign keys and indexes aligned to RAG + chat persistence needs. Include source metadata columns needed by later indexing (`title`, `url`, `agency`, `published_at`, `sha256`, storage path) and avoid adding out-of-scope product analytics fields.</action>
  <verify><automated>supabase db lint --local</automated></verify>
  <done>Migration is syntactically valid and defines all four required Phase 1 tables with relational integrity and useful indexes.</done>
</task>

<task type="auto">
  <name>Task 3: Define scraper metadata models and mapping tests</name>
  <files>scraper/src/polisi_scraper/models.py, scraper/tests/test_config_and_models.py</files>
  <action>Define typed models (`DocumentRecord`, crawl run metadata, adapter output envelope) that encode the agreed DO Spaces hierarchy and filename/versioning strategy from CONTEXT.md. Add focused tests validating path generation, filename suffix behavior for changed documents, and schema-level field completeness.</action>
  <verify><automated>python -m pytest scraper/tests/test_config_and_models.py -q</automated></verify>
  <done>Metadata contracts are enforced by tests and can be consumed by crawler core, adapters, and persistence code without ambiguity.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] python -m pytest scraper/tests/test_config_and_models.py -q
- [ ] supabase db lint --local
- [ ] rg "create table if not exists public.(documents|conversations|messages|citations)" supabase/migrations/20260228_01_phase1_schema.sql
</verification>

<success_criteria>

- All tasks completed
- Config and metadata tests pass
- Required Supabase tables are defined and lint clean
- No credentials or environment-specific values committed to source
  </success_criteria>

<output>
After completion, create `.planning/phases/01-data-corpus/01-01-SUMMARY.md`
</output>
