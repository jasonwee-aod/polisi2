---
phase: 03-product
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/pyproject.toml
  - api/.env.example
  - api/README.md
  - api/src/polisi_api/config.py
  - api/src/polisi_api/main.py
  - api/src/polisi_api/auth.py
  - api/src/polisi_api/models.py
  - api/tests/test_app_contracts.py
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
user_setup:
  - service: supabase
    why: "The Python API needs the project URL and a server-side database connection to verify auth context and persist chats"
    env_vars:
      - name: SUPABASE_URL
        source: "Supabase dashboard -> Project URL"
      - name: SUPABASE_DB_URL
        source: "Supabase dashboard -> Project Settings -> Database connection string"
must_haves:
    truths:
      - "A runnable FastAPI service exists with typed request/response contracts for chat, citations, and conversation history."
      - "Authenticated requests are bound to the Supabase user identity on the server rather than trusting client-supplied user IDs."
      - "The API runtime has one documented environment contract for local development and droplet deployment."
    artifacts:
      - path: "api/src/polisi_api/main.py"
        provides: "FastAPI application entrypoint with health and router wiring"
        contains: "FastAPI("
      - path: "api/src/polisi_api/auth.py"
        provides: "Bearer-token verification and current-user dependency for Supabase-backed requests"
        contains: "def get_current_user"
      - path: "api/src/polisi_api/models.py"
        provides: "Typed chat, citation, and conversation DTOs shared by later routes"
        contains: "class ChatRequest"
      - path: "api/.env.example"
        provides: "Documented runtime contract for Supabase and Anthropic-facing settings"
        contains: "SUPABASE_URL="
    key_links:
      - from: "api/src/polisi_api/auth.py"
        to: "api/src/polisi_api/main.py"
        via: "route dependencies use the verified Supabase user context"
      - from: "api/src/polisi_api/models.py"
        to: "api/tests/test_app_contracts.py"
        via: "contract tests pin the chat and citation schema before implementation"
      - from: "api/.env.example"
        to: "api/src/polisi_api/config.py"
        via: "environment variable names and defaults must stay aligned"
---

<objective>
Create the Python API foundation and auth contract for the Phase 3 product.

Purpose: The product cannot be planned as a reliable end-to-end system until the backend runtime, auth boundary, and response contracts are fixed.
Output: FastAPI scaffold, Supabase-backed auth dependency, typed API models, and executable API contract tests.
</objective>

<execution_context>
@/Users/jasonwee/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jasonwee/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-product/03-CONTEXT.md
@infrastructure.md
@supabase/migrations/20260228_01_phase1_schema.sql
@supabase/migrations/20260228_02_phase2_documents_chunks.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the Phase 3 FastAPI package and runtime contract</name>
  <files>api/pyproject.toml, api/.env.example, api/README.md, api/src/polisi_api/config.py, api/src/polisi_api/main.py</files>
  <action>Create a standalone `api/` Python package for the product service using FastAPI, Pydantic, Uvicorn, and the Anthropic SDK, while reusing direct Postgres access patterns already established in the repo. Define a centralized settings loader for Supabase URL, direct DB URL, Anthropic key/model, retrieval limits, and allowed frontend origin(s). Add a minimal FastAPI app entrypoint with a `/healthz` route and router include points so later plans can add chat and conversation routes without reshaping the package.</action>
  <verify><automated>python -m pytest api/tests/test_app_contracts.py::test_settings_and_health_contract -q</automated></verify>
  <done>The repository contains a runnable Python API package with documented env vars and a passing smoke test proving the service starts and exposes a health endpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Add Supabase-authenticated request identity plumbing</name>
  <files>api/src/polisi_api/auth.py, api/src/polisi_api/config.py, api/src/polisi_api/main.py, api/tests/test_app_contracts.py</files>
  <action>Add an auth module that accepts the bearer token coming from the logged-in web app, verifies it against Supabase-issued identity material, and exposes a `get_current_user` dependency returning the authenticated user id plus basic profile claims. Do not trust user ids passed in request bodies or query params. Keep the verification path cacheable and deterministic for tests, with a test seam for fake keys/tokens so the executor can prove the auth boundary without live Supabase traffic on every run.</action>
  <verify><automated>python -m pytest api/tests/test_app_contracts.py::test_current_user_dependency_rejects_missing_or_invalid_tokens -q</automated></verify>
  <done>API routes can require a verified Supabase user identity, and unauthenticated or invalid requests are rejected before any chat persistence or retrieval logic runs.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the API request and response schemas for chat and history</name>
  <files>api/src/polisi_api/models.py, api/src/polisi_api/main.py, api/tests/test_app_contracts.py</files>
  <action>Define the typed contracts the rest of Phase 3 will implement: chat request, streaming event envelope, assistant response payload, citation record, conversation summary, and conversation detail/message history. The response schema must explicitly support inline citation indices, language codes (`ms` / `en`), conversation/message ids, and metadata needed by the frontend citation side panel. Add tests that pin these models and ensure the FastAPI OpenAPI schema exposes the expected fields.</action>
  <verify><automated>python -m pytest api/tests/test_app_contracts.py::test_openapi_exposes_chat_citation_and_history_models -q</automated></verify>
  <done>The product has stable backend contracts for chat, citations, and conversation history, and later plans can implement behavior against those contracts without renegotiating shapes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] python -m pytest api/tests/test_app_contracts.py -q
- [ ] rg "SUPABASE_URL|SUPABASE_DB_URL|ANTHROPIC_API_KEY" api/.env.example api/src/polisi_api/config.py
- [ ] rg "def get_current_user|class ChatRequest|class CitationRecord" api/src/polisi_api/auth.py api/src/polisi_api/models.py
</verification>

<success_criteria>

- All tasks completed
- FastAPI runtime and env contract exist in `api/`
- Supabase-authenticated requests resolve to a trusted backend user context
- Chat, citation, and history schemas are fixed and test-covered before behavior is added
  </success_criteria>

<output>
After completion, create `.planning/phases/03-product/03-01-SUMMARY.md`
</output>
