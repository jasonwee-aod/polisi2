---
phase: 03-product
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/tsconfig.json
  - web/next.config.ts
  - web/.env.example
  - web/app/layout.tsx
  - web/app/page.tsx
  - web/app/auth/page.tsx
  - web/app/(app)/layout.tsx
  - web/components/auth/auth-form.tsx
  - web/components/app/app-shell.tsx
  - web/lib/supabase/client.ts
  - web/lib/supabase/server.ts
  - web/middleware.ts
  - web/tests/auth_shell.test.tsx
autonomous: true
requirements:
  - FE-01
user_setup:
  - service: supabase
    why: "The Next.js app needs the public Supabase project keys to perform browser and server auth flows"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase dashboard -> Project URL"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase dashboard -> Project Settings -> API keys"
must_haves:
    truths:
      - "A user can sign up with email/password, log in from the same auth surface, and stay in an authenticated app shell across refresh."
      - "Unauthenticated visitors are routed to auth first instead of seeing a usable chat page."
      - "Successful sign-up lands the user directly in a minimal empty product shell that hints BM questions are supported."
    artifacts:
      - path: "web/app/auth/page.tsx"
        provides: "Combined log in / sign up entrypoint"
        contains: "AuthPage"
      - path: "web/middleware.ts"
        provides: "Auth-first route protection for the chat application"
        contains: "middleware"
      - path: "web/lib/supabase/server.ts"
        provides: "Server-side Supabase client used for session-aware rendering"
        contains: "createServerClient"
      - path: "web/components/app/app-shell.tsx"
        provides: "Authenticated app wrapper with minimal first-run state"
        contains: "BM"
    key_links:
      - from: "web/app/auth/page.tsx"
        to: "web/components/auth/auth-form.tsx"
        via: "the auth page renders one combined email/password form with mode toggling"
      - from: "web/lib/supabase/server.ts"
        to: "web/middleware.ts"
        via: "session-aware route protection stays consistent between middleware and server rendering"
      - from: "web/app/(app)/layout.tsx"
        to: "web/components/app/app-shell.tsx"
        via: "authenticated pages inherit one shared app shell instead of duplicating first-run UI"
---

<objective>
Create the Next.js product shell and Supabase auth flow for the Phase 3 web app.

Purpose: The user-facing product needs a protected, familiar entry path before chat behavior is added.
Output: Next.js App Router app, Supabase auth wiring, auth-first middleware, and a minimal authenticated shell.
</objective>

<execution_context>
@/Users/jasonwee/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jasonwee/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-product/03-CONTEXT.md
@infrastructure.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the Next.js App Router app and Supabase client helpers</name>
  <files>web/package.json, web/tsconfig.json, web/next.config.ts, web/.env.example, web/app/layout.tsx, web/lib/supabase/client.ts, web/lib/supabase/server.ts</files>
  <action>Initialize a `web/` Next.js App Router project with TypeScript and the standard Supabase SSR helper package so server-rendered auth and client-side auth actions share one session model. Add an env contract for the public Supabase URL, anon key, and backend API base URL, and build a minimal root layout ready to host the authenticated app shell later. Keep the stack lean: no extra state library or design system yet.</action>
  <verify><automated>npm --prefix web run lint</automated></verify>
  <done>The repository has a runnable Next.js app scaffold with documented env vars and reusable browser/server Supabase helpers for auth-aware pages.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the combined auth page and auth-first routing</name>
  <files>web/app/page.tsx, web/app/auth/page.tsx, web/components/auth/auth-form.tsx, web/middleware.ts, web/lib/supabase/server.ts, web/tests/auth_shell.test.tsx</files>
  <action>Build a single auth entry screen that supports log in and sign up via email/password, using Supabase Auth and mode toggling instead of separate pages. Route unauthenticated visitors from `/` and app routes into auth first, and after successful sign up or log in redirect directly into the authenticated chat shell. Preserve the familiar AI-chat feel by keeping the auth surface simple rather than adding onboarding steps or marketing copy.</action>
  <verify><automated>npm --prefix web run test -- web/tests/auth_shell.test.tsx</automated></verify>
  <done>Users can sign up or log in from one page, auth redirects are enforced by middleware, and successful auth lands inside the product shell instead of back on the auth screen.</done>
</task>

<task type="auto">
  <name>Task 3: Add the minimal authenticated shell and first-run empty state</name>
  <files>web/app/(app)/layout.tsx, web/components/app/app-shell.tsx, web/tests/auth_shell.test.tsx</files>
  <action>Create the authenticated application shell that later plans will extend with sidebar and chat content. The first-run state should stay intentionally minimal, mention that questions can be asked in BM, and avoid adding onboarding cards or non-essential product chrome. Keep the shell compatible with persistent sessions across refresh by reading the server session before rendering protected content.</action>
  <verify><automated>npm --prefix web run test -- web/tests/auth_shell.test.tsx</automated></verify>
  <done>Authenticated users see a minimal protected shell with a BM hint, and refresh-safe session handling is established before conversation UI arrives.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm --prefix web run lint
- [ ] npm --prefix web run test -- web/tests/auth_shell.test.tsx
- [ ] rg "NEXT_PUBLIC_SUPABASE_URL|NEXT_PUBLIC_SUPABASE_ANON_KEY|NEXT_PUBLIC_API_BASE_URL" web/.env.example
- [ ] rg "createBrowserClient|createServerClient|middleware" web/lib/supabase/client.ts web/lib/supabase/server.ts web/middleware.ts
</verification>

<success_criteria>

- All tasks completed
- Next.js app scaffold and Supabase helpers exist under `web/`
- Auth is combined into one screen and enforced before chat usage
- Authenticated users land in a minimal BM-aware shell and stay signed in across refresh
  </success_criteria>

<output>
After completion, create `.planning/phases/03-product/03-02-SUMMARY.md`
</output>
