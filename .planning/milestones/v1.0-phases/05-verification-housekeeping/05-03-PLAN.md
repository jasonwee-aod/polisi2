---
phase: 05-verification-housekeeping
plan: "03"
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - api/src/polisi_api/chat/retrieval.py
  - api/tests/test_chat_service.py
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "PostgresRetriever.retrieve() returns [] immediately when embed() returns [] — the psycopg.connect block is never reached"
    - "A 429 from OpenAI embeddings causes the full degradation chain: embed() returns [] -> retrieve() returns [] -> generate_reply() returns build_no_information_text() -> HTTP 200 with graceful message"
    - "An automated test asserts retrieve() returns [] when the embedding client yields an empty list"
  artifacts:
    - path: "api/src/polisi_api/chat/retrieval.py"
      provides: "Empty-embedding guard in PostgresRetriever.retrieve()"
      contains: "if not embedding:"
    - path: "api/tests/test_chat_service.py"
      provides: "Test asserting the OpenAI-429 degradation chain"
      contains: "test_retrieve_returns_empty_list_when_embed_returns_empty"
  key_links:
    - from: "api/src/polisi_api/chat/retrieval.py"
      to: "psycopg.connect"
      via: "if not embedding: return [] guard before connect call"
      pattern: "if not embedding"
    - from: "api/tests/test_chat_service.py"
      to: "PostgresRetriever.retrieve"
      via: "FakeEmbeddingClient returning [] triggers early return"
      pattern: "test_retrieve_returns_empty_list_when_embed_returns_empty"
---

<objective>
Add a two-line empty-embedding guard to PostgresRetriever.retrieve() and a corresponding automated test, closing the critical gap where an OpenAI 429 would propagate as a psycopg DatabaseError (HTTP 500) rather than a graceful degradation response.

Purpose: The VERIFICATION.md (score 7/8) identifies a broken degradation chain. embed() already returns [] on OpenAI 429, but retrieve() blindly passes that to _vector_literal([]) which produces the string "[]" — an invalid zero-dimension pgvector literal. pgvector raises DatabaseError on the cast, causing HTTP 500. The intended chain (embed returns [] -> retrieve returns [] -> build_no_information_text -> HTTP 200) requires a guard before the DB call.

Output: retrieval.py with guard at line 71; test_chat_service.py with a new unit test confirming empty-embedding short-circuit.
</objective>

<execution_context>
@/Users/jasonwee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jasonwee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@api/src/polisi_api/chat/retrieval.py
@api/tests/test_chat_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add empty-embedding guard to PostgresRetriever.retrieve() and add unit test</name>
  <files>
    api/src/polisi_api/chat/retrieval.py
    api/tests/test_chat_service.py
  </files>
  <action>
    **Part A — Fix retrieval.py**

    Open `api/src/polisi_api/chat/retrieval.py`.

    In `PostgresRetriever.retrieve()` (currently at line 69), locate the line:

        embedding = await self._embedding_client.embed(question)

    Immediately after that line (before the `with psycopg.connect(...)` block), insert exactly two lines:

        if not embedding:
            return []

    The resulting method body should start:

        async def retrieve(self, question: str, *, limit: int) -> list[RetrievedChunk]:
            embedding = await self._embedding_client.embed(question)
            if not embedding:
                return []
            with psycopg.connect(self._settings.supabase_db_url) as conn:
                ...

    Do NOT change any other lines in the file. Do NOT add imports. `_vector_literal([])` is never called when embedding is empty — that is the correct invariant.

    **Part B — Add unit test to test_chat_service.py**

    Open `api/tests/test_chat_service.py`.

    At the end of the file, append a new standalone async test exercising the empty-embedding guard:

        @dataclass
        class EmptyEmbeddingClient:
            async def embed(self, text: str) -> list[float]:
                return []


        def test_retrieve_returns_empty_list_when_embed_returns_empty() -> None:
            asyncio.run(_run_retrieve_empty_embedding())


        async def _run_retrieve_empty_embedding() -> None:
            settings = build_settings()
            retriever = PostgresRetriever(
                settings, embedding_client=EmptyEmbeddingClient()
            )
            result = await retriever.retrieve("any question", limit=5)
            assert result == []

    Add the `PostgresRetriever` import to the existing import block at the top of `test_chat_service.py`:

        from polisi_api.chat.retrieval import RetrievedChunk, PostgresRetriever

    The test uses `EmptyEmbeddingClient` (which satisfies the structural protocol accepted by `PostgresRetriever.__init__`'s `embedding_client` parameter) and asserts the early return fires before any DB connection attempt. No live DB or network is required.
  </action>
  <verify>
    <automated>cd /Users/jasonwee/VCP/Polisi2/api && python3.13 -m pytest tests/test_chat_service.py::test_retrieve_returns_empty_list_when_embed_returns_empty -x -v 2>&1 | tail -20</automated>
    <manual>Confirm retrieval.py contains the guard: grep -n "if not embedding" /Users/jasonwee/VCP/Polisi2/api/src/polisi_api/chat/retrieval.py should return line 72 (or nearby). Confirm the guard appears BEFORE the psycopg.connect line.</manual>
    <sampling_rate>Run after this task commits, before writing SUMMARY.md</sampling_rate>
  </verify>
  <done>
    - `if not embedding: return []` guard exists in PostgresRetriever.retrieve() immediately after the embed() call, before the psycopg.connect block
    - `test_retrieve_returns_empty_list_when_embed_returns_empty` passes — retrieve() returns [] when the embedding client yields an empty list, with no DB connection attempted
    - Full test suite `pytest api/tests/test_chat_service.py -v` still passes (no regressions)
  </done>
</task>

</tasks>

<verification>
Run the full API test suite to confirm no regressions:

    cd /Users/jasonwee/VCP/Polisi2/api && python3.13 -m pytest tests/ -v 2>&1 | tail -30

Expected: The new test passes. Pre-existing 5/6 pass rate holds (the one pre-existing failure — `test_current_user_dependency_rejects_missing_or_invalid_tokens` — is unrelated to this change and predates Phase 5).

Confirm the guard is in place:

    grep -n "if not embedding" /Users/jasonwee/VCP/Polisi2/api/src/polisi_api/chat/retrieval.py

Expected: one match at approximately line 72, inside PostgresRetriever.retrieve().
</verification>

<success_criteria>
- retrieval.py line ~72: `if not embedding:` appears immediately after `embedding = await self._embedding_client.embed(question)` and before the `with psycopg.connect(...)` block
- test_chat_service.py contains `test_retrieve_returns_empty_list_when_embed_returns_empty` and it passes with no DB required
- `pytest api/tests/test_chat_service.py` reports the same pass count as before plus 1 new pass
- The degradation chain described in VERIFICATION.md Truth 6 is now complete: OpenAI 429 -> embed() returns [] -> retrieve() returns [] -> generate_reply() sees `not retrieved` -> build_no_information_text() -> HTTP 200
</success_criteria>

<output>
After completion, create `.planning/phases/05-verification-housekeeping/05-03-SUMMARY.md` following the standard summary template.

Then update `.planning/phases/05-verification-housekeeping/05-VERIFICATION.md` frontmatter:
- Change `status: gaps_found` to `status: verified`
- Change `score: 7/8 must-haves verified` to `score: 8/8 must-haves verified`
- Change Truth 6 Status from `FAILED` to `VERIFIED`
- Update the Gaps Summary section to note the gap is closed

Also update `.planning/STATE.md`:
- `current_focus` line to reflect plan 03 complete, all phase 5 gaps closed
- Add to Decisions: "[Phase 05-03-gap-closure]: PostgresRetriever.retrieve() guards against empty embedding with 'if not embedding: return []' before the psycopg.connect block, closing the OpenAI-429 degradation chain"
</output>
